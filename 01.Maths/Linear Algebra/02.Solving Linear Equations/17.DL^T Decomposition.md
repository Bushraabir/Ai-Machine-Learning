
> [!abstract] Core Idea
> $LDL^T$ is **LU factorization exploiting symmetry**. For a symmetric matrix $A = A^T$, the upper triangular factor $U$ is not independent — it is forced to equal $DL^T$. So instead of storing both $L$ and $U$, you store only $L$ and diagonal $D$. Half the work. Same pivots.

You already know $A = LU$. For a **symmetric** matrix ($A = A^T$), there is hidden structure: $U$ is not free — it is completely determined by $L$ and the pivots. $LDL^T$ makes this explicit.

| Factorization   | Applies To                  | Cost        | Requires                         |
| --------------- | --------------------------- | ----------- | -------------------------------- |
| $LU$            | Any square matrix           | $n^3/3$ ops | Non-singular leading submatrices |
| $LDL^T$         | Symmetric                   | $n^3/6$ ops | Same as LU                       |
| Cholesky $LL^T$ | Symmetric Positive Definite | $n^3/6$ ops | All pivots $> 0$                 |

> [!tip] Mental Model
> Think of $D$ as a "mirror" sitting in the middle. The matrix reads the same forwards ($L$) and backwards ($L^T$), just like the symmetric matrix itself.


## 2. The Theorem

> [!theorem] $LDL^T$ Factorization
> If $A$ is symmetric and all leading principal submatrices are non-singular (no row exchanges needed in Gaussian elimination), then:
> $$A = LDL^T$$
> where $L$ is **unit lower triangular** (1s on diagonal) and $D$ is **diagonal**.


## 3. Derivation from LU — First Principles
Start from what you know: $A = LU$.
- **Step 1:** Use symmetry. $LU = A = A^T = (LU)^T = U^T L^T$
- **Step 2:** Factor the diagonal out of $U$. Let $D = \text{diag}(u_{11}, u_{22}, \ldots, u_{nn})$, and write:
	$U = D\tilde{U}$
	where $\tilde{U}$ is unit upper triangular (all diagonal entries = 1).
- **Step 3:** Substitute back: $A = LD\tilde{U}$
- **Step 4:** By the **uniqueness of LU factorization**, matching $LD\tilde{U} = U^T L^T$ forces: $\tilde{U} = L^T$

Therefore: $\boxed{A = LDL^T}$

> [!note] Key Insight
> The uniqueness of LU factorization does the heavy lifting. You don't need a separate proof — the algebra forces $\tilde{U} = L^T$.

## 4. Example: $3 \times 3$ Matrix
$$A = \begin{bmatrix} 4 & 2 & -2 \\ 2 & 2 & -1 \\ -2 & -1 & 3 \end{bmatrix}$$
**Symmetry check:** $a_{12}=a_{21}=2$, $a_{13}=a_{31}=-2$, $a_{23}=a_{32}=-1$ ✓
- **Step 1 : Eliminate Column 1**
	Pivot: $a_{11} = 4$
	Multipliers: $\ell_{21} = \frac{2}{4} = \frac{1}{2}, \qquad \ell_{31} = \frac{-2}{4} = -\frac{1}{2}$
	After row operations: $\begin{bmatrix} 4 & 2 & -2 \\ 0 & 1 & 0 \\ 0 & 0 & 2 \end{bmatrix}$

> [!check] Symmetry at Work
> Notice: after eliminating column 1, the submatrix in rows/cols 2–3 is **also symmetric**. This is always true — eliminating from a symmetric matrix leaves a symmetric Schur complement.

- **Step 2: Eliminate Column 2**
	Pivot: $a_{22}^{(2)} = 1$. Multiplier: $\ell_{32} = 0/1 = 0$. No change.
	$U = \begin{bmatrix} 4 & 2 & -2 \\ 0 & 1 & 0 \\ 0 & 0 & 2 \end{bmatrix}$

-  Step 3: Extract $D$ and $L^T$
	$D = \begin{bmatrix} 4 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 2 \end{bmatrix}$
	$L^T = D^{-1}U = \begin{bmatrix} 1 & 1/2 & -1/2 \\ 0 & 1 & 0 \\ 0 & 0 & 1 \end{bmatrix}$
	$L = \begin{bmatrix} 1 & 0 & 0 \\ 1/2 & 1 & 0 \\ -1/2 & 0 & 1 \end{bmatrix}$

> [!tip] Shortcut
> The entries of $L$ below the diagonal **are exactly the Gaussian elimination multipliers** $\ell_{ij}$. You read them off automatically — no extra computation.
### Verification:
$LD = \begin{bmatrix} 4 & 0 & 0 \\ 2 & 1 & 0 \\ -2 & 0 & 2 \end{bmatrix}$
$(LD)L^T = \begin{bmatrix} 4 & 2 & -2 \\ 2 & 2 & -1 \\ -2 & -1 & 3 \end{bmatrix} = A \checkmark$

## 5. $LDL^T$ and the Positive Definiteness Test:
> $A$ is **positive definite** (PD) if $\mathbf{x}^T A \mathbf{x} > 0$ for all $\mathbf{x} \neq \mathbf{0}$.

> [!theorem] Pivot Test via $LDL^T$
> A symmetric matrix $A$ is positive definite **if and only if** all diagonal entries of $D$ are positive.

### Proof:
$\mathbf{x}^T A \mathbf{x} = \mathbf{x}^T L D L^T \mathbf{x} = \underbrace{(L^T\mathbf{x})^T}_{\mathbf{y}^T} D \underbrace{(L^T\mathbf{x})}_{\mathbf{y}} = \mathbf{y}^T D \mathbf{y}$
Let $\mathbf{y} = L^T \mathbf{x}$. Since $L^T$ is unit upper triangular, it is **invertible**, so as $\mathbf{x}$ ranges over all nonzero vectors, so does $\mathbf{y}$.
$\mathbf{y}^T D \mathbf{y} = \sum_{i=1}^n d_i y_i^2$ -> This is $> 0$ for all $\mathbf{y} \neq \mathbf{0}$ **if and only if** every $d_i > 0$.
$$\boxed{A \text{ is PD} \iff d_1, d_2, \ldots, d_n > 0}$$

### Applied to Our Example
$D = \text{diag}(4, 1, 2)$ — all positive → $A$ **is positive definite** ✓


## 6. Visual: The Factorization Structure

```

A (symmetric) = L × D × Lᵀ

[4 2 -2] [1 0 0] [4 0 0] [1 1/2 -1/2] [2 2 -1] = [1/2 1 0] × [0 1 0] × [0 1 0 ] [-2 -1 3] [-1/2 0 1] [0 0 2] [0 0 1 ]


 ↑                  ↑              ↑              ↑


Symmetric Multipliers Pivots on Transpose (reads same from Gauss diagonal of L top/bottom) elimination

Pivot test shortcut:

Gaussian elimination produces pivots: 4 → 1 → 2 ↓ ↓ ↓ These ARE the d_i entries of D ↓ ↓ ↓ All positive → A is PD ✓

```

---

## 7. Connection Map — How $LDL^T$ Relates to Everything Else

```

LU Factorization │ │ (A is symmetric) ▼ LDL^T Factorization ←──────── Positive Definiteness Test │ (all d_i > 0 ↔ PD) │ (all d_i > 0, │ take √d_i) ▼ Cholesky A = L̃L̃ᵀ where L̃ = L·D^(1/2)

````

| Method | Symmetric? | PD Required? | Conceptual Role |
|---|---|---|---|
| $LU$ | No | No | General factorization |
| $LDL^T$ | **Yes** | No | Symmetric factorization, pivot extraction |
| Cholesky | **Yes** | **Yes** | Numerically optimal for PD systems |

> [!note] Cholesky from $LDL^T$
> When all $d_i > 0$, write $D = D^{1/2} D^{1/2}$ and absorb:
> $$A = L D L^T = (LD^{1/2})(D^{1/2}L^T) = \tilde{L}\tilde{L}^T$$
> The square roots $\sqrt{d_i}$ require $d_i > 0$, hence the PD requirement.

---

## 8. The Formula for $LDL^T$ Entries (Direct Computation)

You don't need to run full LU and then extract — you can compute $L$ and $D$ directly:

For $j = 1, 2, \ldots, n$:

$$d_j = a_{jj} - \sum_{k=1}^{j-1} \ell_{jk}^2 \, d_k$$

For $i > j$:

$$\ell_{ij} = \frac{1}{d_j}\left(a_{ij} - \sum_{k=1}^{j-1} \ell_{ik} \, \ell_{jk} \, d_k\right)$$

> [!example] Trace Through for Our 3×3
> 
> $j=1$: $d_1 = a_{11} = 4$
> 
> $i=2, j=1$: $\ell_{21} = a_{21}/d_1 = 2/4 = 1/2$
> 
> $i=3, j=1$: $\ell_{31} = a_{31}/d_1 = -2/4 = -1/2$
> 
> $j=2$: $d_2 = a_{22} - \ell_{21}^2 d_1 = 2 - (1/4)(4) = 1$
> 
> $i=3, j=2$: $\ell_{32} = (a_{32} - \ell_{31}\ell_{21}d_1)/d_2 = (-1 - (-1/2)(1/2)(4))/1 = (-1+1)/1 = 0$
> 
> $j=3$: $d_3 = a_{33} - \ell_{31}^2 d_1 - \ell_{32}^2 d_2 = 3 - (1/4)(4) - 0 = 2$

Results match perfectly: $D = \text{diag}(4,1,2)$, $L$ as before ✓

---

## 9. Python Implementation

```python
import numpy as np

def ldlt(A):
    """
    Compute LDL^T decomposition of symmetric matrix A.
    Returns L (unit lower triangular) and d (diagonal of D as vector).
    """
    n = A.shape[0]
    L = np.eye(n)
    d = np.zeros(n)
    
    for j in range(n):
        # Compute pivot d_j
        d[j] = A[j, j] - sum(L[j, k]**2 * d[k] for k in range(j))
        
        # Compute multipliers for column j
        for i in range(j+1, n):
            L[i, j] = (A[i, j] - sum(L[i,k] * L[j,k] * d[k] 
                                      for k in range(j))) / d[j]
    
    return L, d

# Test
A = np.array([[4., 2., -2.],
              [2., 2., -1.],
              [-2., -1., 3.]])

L, d = ldlt(A)
D = np.diag(d)

# Verify
print(np.allclose(L @ D @ L.T, A))   # True
print("Pivots (d):", d)               # [4. 1. 2.]
print("PD?", all(d > 0))              # True
````

---

## 10. Common Mistakes & Gotchas

> [!warning] Mistake 1: Confusing $LDL^T$ with Cholesky $LDL^T$ **does not** require positive definiteness. $D$ can have negative or zero entries. Cholesky requires PD because it needs $\sqrt{d_i}$.

> [!warning] Mistake 2: Forgetting symmetry is essential $LDL^T$ only works when $A = A^T$. For non-symmetric matrices, use $LU$.

> [!warning] Mistake 3: Thinking $L$ is different from LU's $L$ It's identical. The multipliers $\ell_{ij}$ from Gaussian elimination populate $L$ in **both** LU and LDL^T. No extra computation.

> [!warning] Mistake 4: Confusing $D$ entries with eigenvalues The diagonal entries of $D$ are the **pivots**, not eigenvalues. However: $A$ is PD iff all eigenvalues $> 0$ iff all pivots $> 0$ — these conditions are equivalent, but $d_i \neq \lambda_i$ in general.

---

## 11. Synergy With Your Curriculum

|Topic|Connection to $LDL^T$|
|---|---|
|**Stat 110 / Probability**|Covariance matrices $\Sigma$ are PSD; $LDL^T$ tests/verifies this computationally|
|**CS229 / ML**|Gaussian distribution requires $\Sigma^{-1}$; Cholesky (from $LDL^T$) is the efficient solver|
|**PRML**|Kernel matrices must be PSD; $LDL^T$ is the numerical check|
|**CS231 / CNNs**|Batch normalization involves covariance; understanding PD is foundational|
|**CLRS (Algorithms)**|$LDL^T$ is $O(n^3/6)$; understanding complexity matters for large-scale solvers|

> [!tip] Mini Project Idea Implement a **multivariate Gaussian sampler** from scratch in Python using your own `ldlt()` function. Given covariance $\Sigma$, compute $LDL^T$, then generate samples via $\mathbf{x} = L D^{1/2} \mathbf{z}$ where $\mathbf{z} \sim \mathcal{N}(0,I)$. This bridges 18.06 → Stat 110 → CS229 in one project.

---

## 12. Summary Card

```
┌─────────────────────────────────────────────────────────┐
│                  LDL^T at a Glance                      │
├─────────────────────────────────────────────────────────┤
│  WHAT:  A = L D L^T  (A symmetric)                      │
│  L   =  unit lower triangular  (same as LU's L)         │
│  D   =  diagonal  (entries = Gaussian pivots)           │
│                                                         │
│  HOW:   Run Gaussian elimination on A.                  │
│         Multipliers → L.  Pivots → D.  Done.            │
│                                                         │
│  TEST:  A is PD  ⟺  all d_i > 0                        │
│                                                         │
│  COST:  n³/6  (half the work of LU)                     │
│                                                         │
│  CHOLESKY connection:  A = (L√D)(√D·L^T) = L̃L̃^T       │
│  (only when all d_i > 0, i.e., A is PD)                 │
└─────────────────────────────────────────────────────────┘
```

---

## Related Notes

- [[LU Factorization]]
- [[Positive Definite Matrices]]
- [[Cholesky Decomposition]]
- [[Gaussian Elimination]]
- [[Eigenvalues and Positive Definiteness]]
- [[Covariance Matrices in ML]]

```

---

A few things to note about this note:

**Callout types used** (`> [!abstract]`, `> [!theorem]`, `> [!tip]`, `> [!warning]`, `> [!example]`, `> [!check]`, `> [!definition]`) all render natively in Obsidian with colored icons.

**The ASCII diagrams** in sections 6 and 12 are intentional — Obsidian renders them in monospace inside code blocks, giving clean visual structure without needing MermaidJS.

**The Python block** is syntax-highlighted automatically by Obsidian.

**Curriculum synergy table** in section 11 is specific to your 11-book + 6-course plan — this is where $LDL^T$ plugs into Stat 110, CS229, and PRML simultaneously.

**Next action:** When you finish Strang Chapter 6 (positive definiteness), come back to section 5 of this note and prove the equivalence of all five PD tests (pivots, eigenvalues, determinants, energy, Cholesky). That synthesis is exam-level and interview-level material.
```