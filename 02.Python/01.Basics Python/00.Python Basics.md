## 1. Syntax & Language Fundamentals

- [[01.Variables, data types (int, float, str, bool, None)]]
- [[02.Type casting and type checking]]
- [[03.Operators]]
- [[04.String formatting]]
- [[05.output input]]
- [[06.Comments and docstrings]]


## 2. Control Flow

- [[07.if elif else]]
- [[08.for loop]]
- [[09.while loops]]
- [[10.break continue pass]]
- [[11.Nested loops and conditionals]]


## 3. Data Structures

- **[[12.Lists]]** 
- **[[13.Tuples]]** 
- **[[14.Dictionaries]]** 
- **[[15.Sets]]** 
- [[16.Comprehensions]] 
- [[17.Nested data structures]]

## 4. Functions

- [[18.Defining and calling functions]]
- [[19.Parameters vs arguments]]
- [[20.Default and keyword arguments]]
- [[21.args and kwargs]]
- [[22.Return values]]
- [[23.local vs global]]
- [[24.Lambda functions]]
- [[25.Recursion]]


## 5. Object-Oriented Programming (OOP)

- Classes and objects
- `__init__`, `self`
- Instance vs class vs static methods
- Attributes: instance vs class
- Inheritance and method overriding
- `super()`
- Dunder/magic methods (`__str__`, `__repr__`, `__len__`, `__eq__`, etc.)
- Encapsulation basics

## 6. Iterators, Generators & Functional Tools

- `iter()` and `next()`
- Generator functions (`yield`)
- Generator expressions
- `map()`, `filter()`, `zip()`, `enumerate()`
- `sorted()`, `reversed()`
- `any()`, `all()`


## 7. Error Handling & Exceptions

- `try / except / else / finally`
- Common exceptions (`ValueError`, `TypeError`, `IndexError`, `KeyError`, etc.)
- Raising exceptions (`raise`)
- Custom exception classes


## 8. File I/O

- Reading and writing `.txt` and `.csv` files
- `open()`, `read()`, `readline()`, `readlines()`, `write()`
- Context manager (`with` statement)
- Working with file paths


## 9. Modules & Packages

- Importing modules (`import`, `from x import y`, `as`)
- Writing your own modules
- `__name__ == "__main__"` pattern
- Understanding `__init__.py` and package structure

## 10. Python Memory & Execution Model

- Mutability vs immutability in depth
- Shallow vs deep copy
- How Python manages memory (reference counting basics)
- Pass-by-object-reference behavior
- Variable unpacking and assignment mechanics


## 11. Decorators & Context Managers

- First-class functions and closures
- Writing and applying decorators (`@decorator`)
- `@staticmethod`, `@classmethod`, `@property`
- Writing custom context managers (`__enter__`, `__exit__`)


## 12. Pythonic Coding Patterns

- Ternary expressions
- Walrus operator (`:=`)
- Chained comparisons
- String methods (`.split()`, `.join()`, `.strip()`, `.replace()`, etc.)
- `collections` module basics (`defaultdict`, `Counter`, `deque`)
- `itertools` basics (`product`, `combinations`, `permutations`)


## Priority Order for AI/ML Path

```
Phase 1 (Must master first):       1 -> 2 -> 3 -> 4
Phase 2 (Before touching NumPy):   5 -> 6 -> 7 -> 8
Phase 3 (Before writing ML code):  9 -> 10 -> 11 -> 12
```

Once you complete these three phases, you are ready to drop into NumPy, Pandas, and then PyTorch/TensorFlow with zero friction — because every abstraction those libraries use maps directly back to these fundamentals.