> _"NumPy is not just a library. It is the substrate on which all of scientific Python is built. Every tensor in PyTorch, every matrix in scikit-learn, every array in Pandas traces its lineage here."_

This is the index for NumPy — the numerical computing backbone of the entire AI/ML Python ecosystem. The sequencing below is intentional: master array creation before you slice, slicing before broadcasting, broadcasting before linear algebra. Skip nothing.


## ✦ How to Use This Index

```
Read the note → Derive the behavior by hand → Implement from scratch → Verify against PyTorch equivalent → Move on
```

---

## 1. The NumPy Array Model

- [[01.The ndarray object]]
- [[02.dtype — data types and precision (float32, float64, int32, int64, bool, complex)]]
- [[03.Shape, ndim, size, itemsize, nbytes]]
- [[04.C-order vs Fortran-order (row-major vs column-major memory layout)]]
- [[05.Views vs copies — when does NumPy share memory]]

## 2. Array Creation

- [[06.np.array() and np.asarray()]]
- [[07.np.zeros(), np.ones(), np.full()]]
- [[08.np.eye() and np.identity()]]
- [[09.np.arange() and np.linspace()]]
- [[10.np.random — random number generation (seed, rand, randn, randint, choice)]]
- [[11.np.empty() and uninitialized arrays]]
- [[12.np.fromfunction() and np.fromiter()]]
- [[13.Creating arrays from lists, tuples, and nested structures]]

## 3. Indexing and Slicing

- [[14.Basic indexing — integer and slice]]
- [[15.Multi-dimensional indexing]]
- [[16.Boolean indexing and masking]]
- [[17.Fancy indexing — integer array indexing]]
- [[18.np.where() — conditional selection]]
- [[19.np.take() and np.put()]]
- [[20.Ellipsis (...) and np.newaxis]]

## 4. Shape Manipulation

- [[21.reshape() and ravel() and flatten()]]
- [[22.transpose() and .T]]
- [[23.np.squeeze() and np.expand_dims()]]
- [[24.np.concatenate(), np.stack(), np.vstack(), np.hstack(), np.dstack()]]
- [[25.np.split(), np.hsplit(), np.vsplit()]]
- [[26.np.tile() and np.repeat()]]

## 5. Broadcasting

- [[27.The broadcasting rules — rank, shape compatibility, dimension expansion]]
- [[28.Broadcasting examples — scalar, vector, matrix combinations]]
- [[29.Memory efficiency of broadcasting vs explicit tiling]]
- [[30.Common broadcasting errors and how to debug them]]
- [[31.Broadcasting in ML — batch operations, bias addition, normalisation]]

## 6. Universal Functions (ufuncs)

- [[32.What is a ufunc — element-wise operation on ndarrays]]
- [[33.Arithmetic ufuncs — np.add, np.subtract, np.multiply, np.divide, np.power, np.mod]]
- [[34.Comparison ufuncs — np.equal, np.greater, np.less, np.maximum, np.minimum]]
- [[35.Mathematical ufuncs — np.sqrt, np.exp, np.log, np.log2, np.log10, np.abs, np.sign]]
- [[36.Trigonometric ufuncs — np.sin, np.cos, np.tan, np.arcsin, np.arccos, np.arctan]]
- [[37.np.clip() — bounding values]]
- [[38.ufunc methods — reduce(), accumulate(), outer(), at()]]

## 7. Aggregations and Reductions

- [[39.np.sum(), np.prod() — with axis argument]]
- [[40.np.mean(), np.median(), np.std(), np.var()]]
- [[41.np.min(), np.max(), np.argmin(), np.argmax()]]
- [[42.np.cumsum() and np.cumprod()]]
- [[43.np.any(), np.all()]]
- [[44.np.unique() — finding unique elements and counts]]
- [[45.np.sort() and np.argsort()]]
- [[46.np.percentile() and np.quantile()]]
- [[47.np.histogram() and np.bincount()]]

## 8. Linear Algebra — np.linalg

- [[48.np.dot() and np.matmul() and the @ operator]]
- [[49.Element-wise vs matrix multiplication — critical distinction]]
- [[50.np.linalg.inv() — matrix inverse]]
- [[51.np.linalg.det() — determinant]]
- [[52.np.linalg.eig() and np.linalg.eigh() — eigenvalues and eigenvectors]]
- [[53.np.linalg.svd() — Singular Value Decomposition]]
- [[54.np.linalg.solve() — solving linear systems Ax = b]]
- [[55.np.linalg.lstsq() — least squares solution]]
- [[56.np.linalg.norm() — vector and matrix norms]]
- [[57.np.linalg.matrix_rank()]]
- [[58.np.trace() and np.diag()]]
- [[59.np.outer() — outer product]]
- [[60.np.kron() — Kronecker product]]

## 9. Random Module — np.random

- [[61.np.random.seed() and reproducibility]]
- [[62.Uniform distributions — rand(), random(), uniform()]]
- [[63.Normal distributions — randn(), normal()]]
- [[64.Integer sampling — randint(), choice()]]
- [[65.Probability distributions — binomial, poisson, exponential, beta, gamma, dirichlet]]
- [[66.np.random.shuffle() and np.random.permutation()]]
- [[67.The Generator API (np.random.default_rng) — preferred modern interface]]

## 10. Structured Arrays and Advanced Dtypes

- [[68.Structured arrays — records with named fields]]
- [[69.np.zeros() with custom dtype]]
- [[70.Record arrays — np.recarray]]
- [[71.Complex number support in NumPy]]

## 11. Input and Output

- [[72.np.save() and np.load() — .npy format]]
- [[73.np.savez() and np.savez_compressed() — multiple arrays]]
- [[74.np.savetxt() and np.loadtxt() — plain text]]
- [[75.np.genfromtxt() — handling missing values in text files]]

## 12. Performance and Memory

- [[76.Vectorisation — replacing Python loops with array operations]]
- [[77.Contiguous vs non-contiguous arrays — np.ascontiguousarray()]]
- [[78.In-place operations — out= parameter, += semantics]]
- [[79.np.einsum() — Einstein summation notation]]
- [[80.Memory mapping — np.memmap for large arrays]]
- [[81.Profiling NumPy code — where time actually goes]]

## 13. Polynomials and Interpolation

- [[82.np.poly1d and np.polyfit — polynomial fitting]]
- [[83.np.polyval — evaluating polynomials]]
- [[84.np.roots() — finding polynomial roots]]
- [[85.np.interp() — 1D linear interpolation]]

## 14. Fourier Transform — np.fft

- [[86.np.fft.fft() and np.fft.ifft() — 1D DFT]]
- [[87.np.fft.fft2() — 2D DFT for images]]
- [[88.np.fft.fftfreq() and np.fft.fftshift()]]
- [[89.Applications in signal processing and CNNs]]

## 15. NumPy in the AI/ML Stack

- [[90.NumPy arrays as the interface to PyTorch Tensors and TensorFlow Tensors]]
- [[91.Implementing forward pass of a neural network in pure NumPy]]
- [[92.Implementing backpropagation in pure NumPy]]
- [[93.Gradient descent in NumPy — linear regression from scratch]]
- [[94.Implementing softmax, sigmoid, ReLU, and their derivatives in NumPy]]
- [[95.Batch normalisation from scratch in NumPy]]
- [[96.Implementing PCA via np.linalg.svd()]]
- [[97.Implementing K-Means in NumPy]]
- [[98.Implementing a confusion matrix and classification metrics in NumPy]]
- [[99.np.einsum() patterns used in attention mechanisms]]

---

## Priority Order for AI/ML Path

```
Phase 1 (Absolute foundation):       1 → 2 → 3 → 4
Phase 2 (Numerical reasoning):       5 → 6 → 7
Phase 3 (Linear algebra core):       8 → 9
Phase 4 (Performance and idioms):    10 → 11 → 12
Phase 5 (ML implementations):        13 → 14 → 15
```

Complete Phase 1–3 before touching Pandas or scikit-learn. Complete Phase 4–5 before starting PyTorch — every tensor operation you will ever write maps back to what you learned here.

---

## Synergy Map

|NumPy Topic|Where It Appears in ML/DL|
|---|---|
|`matmul`, `dot`, `@`|Every linear layer, attention head|
|Broadcasting|Bias addition, batch normalisation, loss computation|
|`linalg.svd`|PCA, pseudoinverse, dimensionality reduction|
|`linalg.eig`|PCA via covariance, spectral methods|
|`linalg.solve`|Normal equation for linear regression|
|`einsum`|Multi-head attention, tensor contractions|
|`random.randn`|Weight initialisation (Xavier/He derive from this)|
|Boolean masking|Padding masks, attention masks in Transformers|
|`np.fft`|Convolutional layers in frequency domain|
|`np.clip`|Gradient clipping, reward clipping in RL|

---

